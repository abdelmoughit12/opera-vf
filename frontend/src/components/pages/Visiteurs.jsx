import React from 'react';
import VisiteurPage from '../visiteur/VisiteurPage';

/**
 * Composant Visiteurs (Wrapper)
 * 
 * Ce composant est un simple wrapper qui utilise la page principale VisiteurPage.
 * Il permet de maintenir la compatibilité avec les routes existantes.
 */
const Visiteurs = () => {
  return <VisiteurPage />;
};

export default Visiteurs; 